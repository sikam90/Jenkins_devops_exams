apiVersion: v1
kind: Service
metadata:
  name: cast-service
  namespace: dev
spec:
  selector:
    app: cast-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
